/*
Queue is the data structure that has the first in and first out so the element that has the element first will exit first
*/
#include<vector>
#include<iostream>
#include<queue>
using namespace std;

int main(){
    queue<int> q;
    q.push(2);
    q.pop();
    q.empty();
}